# =============================================================================
# ZX Bank Conversational AI — Python Dependencies
# Python 3.10+ | Works on Windows/macOS/Linux | GPU optional (auto-detected)
# =============================================================================

# --- API Framework -----------------------------------------------------------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# --- LLM Client (OpenAI-compatible SDK for Requesty) ------------------------
openai>=1.50.0

# --- NLP & Embeddings --------------------------------------------------------
sentence-transformers>=3.0.0
scikit-learn>=1.3.0

# --- Vector Search & Sparse Retrieval ----------------------------------------
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# --- Document Processing -----------------------------------------------------
langchain-text-splitters>=0.2.0

# --- Data Validation & Configuration -----------------------------------------
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# --- Observability & Logging -------------------------------------------------
structlog>=24.0.0
rich>=13.0.0

# --- HTTP Client -------------------------------------------------------------
httpx>=0.26.0

# --- PyTorch (REQUIRED — install separately if not present) ------------------
# CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121
# The system auto-detects GPU/CPU — no configuration needed.
# If torch is already installed (any version >= 2.0), it will work.
